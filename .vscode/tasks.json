{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "startRemoteDebug",
            "command": "sh startRemoteDebug.sh", // Could be any other shell command
            "type": "shell"
        }
    ]
}